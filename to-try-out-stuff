import time
import random


#points = 0 

#def ahem(): 
 # print(f"you had {points} points,")
 # points = points + random.randint(18, 42)
  #print(f" but now {points} points! ")

#starttime = time.monotonic()
#while True:
   # ahem()
    #time.sleep(2 - ((time.monotonic() - starttime) % 2))


def func():
    global a
    a = a + 1
    print(a)

a = 1
func()