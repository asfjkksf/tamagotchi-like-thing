import time
import random
points = 1
interval = 3 
min = 2

def func():
  global points
  points = points + 0.01
  print(points)

def func2():
  global points
  points = points
  print(points)

while True:
  time.sleep(interval)
  if random.randint(0, 100) > min:
   func()
  else:
   func2()